/**
 * Le package controller contient les classesDAO et la classe Main. Il regroupe les classes qui effectuent un traitement (ou un controle) sur les données des classes que l'on retrouve dans le package Modèle.
 */
package controller;
